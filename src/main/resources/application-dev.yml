server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/authApp
    username: root
    password: Inal@014!

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true

#jpa and hibernate configuration
logging:
  level:
    org.springframework.security: DEBUG

security:
  jwt:
    #I have passed env as well as my own secret code
  security:
    jwt:

      #  256-bit (32 characters) secret code
      secret: ${JWT_SECRET:Inalidciwcbcowueiuocccdcdqwdrthjhgfdfghbvc-cd}
      issuer: ${JWT_ISSUER:api.substring.com}
      access-ttl-seconds: ${JWT_ACCESS_TTL_SECONDS:900}
      refresh-ttl-seconds: ${JWT_REFRESH_TTL_SECONDS:86400}
      refresh-tkn-cookie-name: ${JWT_REFRESH_TKN_COOKIE_NAME:refreshToken}
      cookie-secure: ${JWT_COOKIE_SECURE:true}
      cookie-http-only: ${JWT_COOKIE_HTTP_ONLY:true}
      # 6. production default should ideally be 'strict' if API and Client share a domain setup
      cookie-same-site: ${JWT_COOKIE_SAME_SITE:strict}






